package org.heikegani.training.session.values;

import co.com.sofka.domain.generic.Identity;

public class SchemaId extends Identity {
    public SchemaId(String uuid) {
        super(uuid);
    }

    public SchemaId() {
    }
}
